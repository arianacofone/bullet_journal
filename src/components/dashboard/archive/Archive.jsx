// component will render archive list and archived items

import React from 'react';
import { withRouter } from 'react-router';

const Archive = () => {
  return (
    <div>
      <h4>Archive is rendering</h4>
    </div>
  );
};

export default withRouter(Archive);
